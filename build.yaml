name: Build Trackball

on:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      include: >
        [
          { "board": "seeeduino_xiao_ble", "shield": "trackball" }
        ]
